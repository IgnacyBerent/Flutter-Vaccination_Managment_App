import 'package:flutter/material.dart';

class VaccinesScreen extends StatelessWidget {
  const VaccinesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Vaccines');
  }
}
